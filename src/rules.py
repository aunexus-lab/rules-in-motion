def check_rule(input_data):
    """
    Applies rules to the input data.
    Returns a decision and an explanation.
    """
    return {
        "decision": "UNKNOWN",
        "reason": "No rules implemented yet"
    }
